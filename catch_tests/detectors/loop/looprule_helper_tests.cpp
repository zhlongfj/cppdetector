#include "../../catch.hpp"
#include "detectors/loop/looprule_helper.h"

SCENARIO("LoopRuleHelper", "") {
    GIVEN("") {
        WHEN("") {
            THEN("no explicit") {
                LoopRuleHelper helper;
                //CHECK(helper.findNotAutoFromFor
            }
        }
    }
}